# WHMCS Admin Panel

Сучасна адміністративна панель для WHMCS з підтримкою мультимовності, побудована на Django з PostgreSQL базою даних.

## ✨ Особливості

- 🔐 **Безпечна адмін панель** - відключена стандартна Django адмінка
- 🌐 **Мультимовність** - підтримка англійської та української мов
- 🐳 **Docker Ready** - повна контейнеризація для розробки та продакшн
- 🛠️ **Dev Tools** - інструменти для розробки та роботи з перекладами
- 📱 **Responsive UI** - адаптивний інтерфейс на Bootstrap 5
- 🔄 **Auto Compilation** - автоматична компіляція перекладів в Docker

## 📁 Структура проекту

```
whmcs-admin-panel/
├── 📂 src/                          # Django додаток
│   ├── 📂 whmcs_project/           # Основні налаштування Django
│   │   ├── settings.py             # Налаштування проекту
│   │   ├── urls.py                 # URL маршрути
│   │   └── wsgi.py                 # WSGI конфігурація
│   ├── 📂 admin_panel/             # Головний додаток адмін панелі
│   │   ├── 📂 templates/           # HTML шаблони
│   │   ├── 📂 management/          # Django команди
│   │   ├── views.py                # Контролери
│   │   └── urls.py                 # URL маршрути додатку
│   ├── 📂 locale/                  # Файли перекладів
│   │   ├── 📂 en/LC_MESSAGES/      # Англійські переклади
│   │   └── 📂 uk/LC_MESSAGES/      # Українські переклади
│   ├── manage.py                   # Django management команди
│   ├── .env                        # Змінні оточення (локально)
│   └── .env.example                # Приклад змінних оточення
├── 📂 docker/                      # Docker конфігурація
│   ├── Dockerfile                  # Основний Docker образ
│   ├── Dockerfile.prod             # Продакшн Docker образ
│   ├── docker-compose.yml          # Оркестрація сервісів
│   ├── check_translations.sh       # Валідація перекладів в Docker
│   └── README.md                   # Docker документація
├── 📂 dev_tools/                   # Інструменти розробки (не в Docker)
│   ├── 📂 translations/            # Інструменти для перекладів
│   │   ├── update_translations.sh  # Оновлення перекладів
│   │   ├── add_language.sh         # Додавання нової мови
│   │   ├── test_translations.sh    # Тестування перекладів
│   │   └── README.md               # Документація по перекладам
│   └── README.md                   # Загальна документація dev_tools
├── 📂 admin_panel/                 # Django додаток (legacy структура)
├── 📂 venv/                        # Python віртуальне оточення
├── requirements.txt                # Python залежності
├── Makefile                        # Команди для зручного управління
├── LANGUAGE_SETUP.md               # Документація по мультимовності
├── .dockerignore                   # Виключення для Docker
├── .gitignore                      # Виключення для Git
└── README.md                       # Ця документація
```

## 🛠️ Технології

- **Python 3.13** - мова програмування
- **Django 6.0** - веб-фреймворк
- **PostgreSQL 16** - база даних
- **Docker & Docker Compose** - контейнеризація
- **Bootstrap 5** - UI фреймворк
- **Font Awesome** - іконки
- **gettext** - інтернаціоналізація

## 🚀 Швидкий старт

### Використання Makefile (рекомендовано)

```bash
# Клонувати репозиторій
git clone <repository-url>
cd whmcs-admin-panel

# Показати всі доступні команди
make help

# Запустити розробницьке середовище
make dev-build

# Проект буде доступний на http://localhost:8000
```

### 1. Повний запуск в Docker

```bash
# Розробка
make dev              # Запуск без збірки
make dev-build        # Збірка та запуск
make logs             # Перегляд логів

# Продакшн
make prod             # Запуск продакшн версії
make prod-build       # Збірка та запуск продакшн

# Переклади
make translations     # Оновити всі переклади
make translations-uk  # Тільки українську
make add-lang LANG=de NAME=Deutsch  # Додати нову мову
```

### 2. Локальна розробка

```bash
# 1. Запустити тільки PostgreSQL
make dev  # або docker-compose -f docker/docker-compose.yml up db

# 2. Активувати віртуальне оточення
source venv/bin/activate

# 3. Встановити залежності
pip install -r requirements.txt

# 4. Перейти в папку Django
cd src

# 5. Виконати міграції
python manage.py migrate

# 6. Створити адміністратора
python manage.py create_admin

# 7. Запустити сервер
python manage.py runserver

# Проект буде доступний на http://localhost:8000
```

## Корисні команди

### Docker команди
```bash
# Запустити сервіси
docker-compose up

# Запустити в фоновому режимі
docker-compose up -d

# Перебудувати образи
docker-compose up --build

# Зупинити сервіси
docker-compose down

# Переглянути логи
docker-compose logs web
docker-compose logs db
```

### Django команди
```bash
# Створити міграції
python manage.py makemigrations

# Застосувати міграції
python manage.py migrate

# Створити суперкористувача
python manage.py createsuperuser

# Запустити сервер розробки
python manage.py runserver

# Запустити Django shell
python manage.py shell
```

## Налаштування бази даних

### Параметри підключення до PostgreSQL:
- **Host**: localhost (для локальної розробки) або db (в Docker)
- **Port**: 5432
- **Database**: whmcs_db
- **Username**: whmcs_user
- **Password**: whmcs_password

### Підключення до БД через psql:
```bash
# Якщо PostgreSQL запущений в Docker
docker-compose exec db psql -U whmcs_user -d whmcs_db
```

## Змінні оточення

Скопіюйте `.env.example` в `.env` та налаштуйте змінні під ваші потреби:

```bash
cp src/.env.example src/.env
```

## 🌐 Мультимовність

Проект підтримує мультимовність з можливістю легкого додавання нових мов.

### Підтримувані мови
- 🇺🇸 **English** - мова за замовчуванням
- 🇺🇦 **Українська** - повний переклад (97% готовності)

### Робота з перекладами

```bash
# Додати новий текст для перекладу в шаблон
{% trans "Your text here" %}

# Оновити переклади
make translations

# Додати нову мову
make add-lang LANG=fr NAME=Français

# Перевірити якість перекладів
make translations-test
```

**Детальна документація:** [LANGUAGE_SETUP.md](LANGUAGE_SETUP.md)

## 🛠️ Dev Tools

Проект включає структуровані інструменти розробки в папці `dev_tools/`:

### Translations Tools
- `update_translations.sh` - оновлення та компіляція перекладів
- `add_language.sh` - додавання нової мови
- `test_translations.sh` - перевірка якості перекладів

**Документація:** [dev_tools/translations/README.md](dev_tools/translations/README.md)

### Майбутні інструменти
Планується додати:
- Database tools (backup, restore, migrate)
- Testing tools (unit tests, integration tests)
- Deployment tools (production deploy scripts)
- Security tools (vulnerability scanning)

## 🐳 Docker

### Конфігурації
- **Dockerfile** - основний образ для розробки
- **Dockerfile.prod** - продакшн образ з оптимізаціями
- **docker-compose.yml** - оркестрація сервісів

### Особливості Docker збірки
- ✅ Автоматична валідація перекладів
- ✅ Компіляція .po файлів в .mo
- ✅ Перевірка синтаксису перекладів
- ✅ Статистика готовності перекладів
- ✅ Health checks для моніторингу

**Документація:** [docker/README.md](docker/README.md)

## 🔧 Розробка

### Workflow для нових функцій
1. Створіть гілку: `git checkout -b feature/new-feature`
2. Активуйте віртуальне оточення: `source venv/bin/activate`
3. Встановіть залежності: `pip install package_name`
4. Оновіть requirements.txt: `pip freeze > requirements.txt`
5. Додайте переклади для нових текстів
6. Протестуйте зміни: `make translations-test`
7. Створіть міграції: `python manage.py makemigrations`
8. Протестуйте в Docker: `make dev-build`

### Contribution Guidelines

#### Додавання нових dev tools
1. Створіть папку в `dev_tools/category/`
2. Додайте README.md з документацією
3. Додайте скрипти з правильними правами (`chmod +x`)
4. Оновіть головний README.md
5. Додайте команди в Makefile (якщо потрібно)

#### Робота з перекладами
1. Використовуйте `{% trans %}` для всіх текстів
2. Запускайте `make translations` після додавання текстів
3. Заповнюйте переклади в .po файлах
4. Тестуйте з `make translations-test`

#### Docker збірка
- Переклади компілюються автоматично
- Використовуйте `make build` для тестування збірки
- Перевіряйте логи збірки на помилки перекладів

## 📚 Документація

- [LANGUAGE_SETUP.md](LANGUAGE_SETUP.md) - налаштування мультимовності
- [dev_tools/README.md](dev_tools/README.md) - інструменти розробки
- [dev_tools/translations/README.md](dev_tools/translations/README.md) - робота з перекладами
- [docker/README.md](docker/README.md) - Docker конфігурація
- [CHANGELOG.md](CHANGELOG.md) - історія змін
