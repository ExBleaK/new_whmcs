# Dev Tools для WHMCS Admin Panel

Ця папка містить інструменти для розробки, які не потрапляють в Docker контейнер.

## Структура

```
dev_tools/
├── README.md                    # Ця документація
├── translations.py              # Єдиний Python скрипт для перекладів
└── [майбутні інструменти]      # Інші dev інструменти
```

## Категорії інструментів

### 🌐 Translations (Переклади)
Єдиний Python скрипт для роботи з мультимовністю та інтернаціоналізацією.

**Основні команди:**
```bash
# Показати допомогу
python dev_tools/translations.py help

# Оновити всі переклади
python dev_tools/translations.py update

# Оновити конкретну мову
python dev_tools/translations.py update uk

# Перевірити якість перекладів
python dev_tools/translations.py test

# Показати статистику
python dev_tools/translations.py stats

# Виправити англійські переклади
python dev_tools/translations.py fix-english

# Додати нову мову
python dev_tools/translations.py add de Deutsch
```

**Переваги:**
- Єдиний інструмент для всіх операцій
- Кращі повідомлення про помилки
- Детальна статистика з прогрес-барами
- Кольоровий вивід
- Валідація параметрів
- Підтримка аргументів командного рядка

**Підтримувані мови:**
- **en** (English) - мова за замовчуванням
- **uk** (Українська) - повний переклад

### 📊 Development Dashboard
Веб-інтерфейс для моніторингу проекту (доступний тільки в DEBUG режимі).

**Доступ:**
- Локально: `http://localhost:8001/dev/`
- Docker: `http://localhost:8000/dev/`
- З admin панелі: кнопка "Dev Tools"

**Функції:**
- URL Patterns - всі маршрути проекту
- Django Apps - інформація про додатки та моделі
- Database Info - статистика БД та таблиці
- Translations - статус перекладів з прогресом
- Settings - безпечний перегляд налаштувань
- System Info - інформація про середовище

### 🔧 Майбутні категорії
Планується додати інструменти для:

- **Database** - скрипти для роботи з базою даних
- **Testing** - інструменти для тестування
- **Deployment** - скрипти для деплою
- **Monitoring** - інструменти моніторингу
- **Security** - перевірки безпеки

## Швидкий старт

### Робота з перекладами
```bash
# 1. Додати новий текст в шаблон
{% trans "New text" %}

# 2. Оновити переклади
python dev_tools/translations.py update

# 3. Відредагувати файл перекладу
# src/locale/uk/LC_MESSAGES/django.po

# 4. Перекомпілювати
python dev_tools/translations.py update

# 5. Перезапустити сервер
```

### Додавання нової мови
```bash
# Додати німецьку мову
python dev_tools/translations.py add de Deutsch

# Перевірити результат
python dev_tools/translations.py test

# Показати статистику
python dev_tools/translations.py stats
```

### Структура файлів перекладів
```
src/locale/
├── en/LC_MESSAGES/
│   ├── django.po    # Вихідний файл перекладів
│   └── django.mo    # Скомпільований файл
└── uk/LC_MESSAGES/
    ├── django.po    # Український переклад
    └── django.mo    # Скомпільований файл
```

### Технічні деталі
**Формат файлів .po:**
```po
#: admin_panel/templates/admin_panel/base.html:20
msgid "Dashboard"
msgstr "Панель управління"
```

**Налагодження:**
```bash
# Перевірка синтаксису
msgfmt --check-format src/locale/uk/LC_MESSAGES/django.po

# Ручна компіляція
cd src && python manage.py compilemessages --verbosity=2

# Пошук непереведених рядків
grep -n "msgstr \"\"$" src/locale/uk/LC_MESSAGES/django.po
```

## Інтеграція з Makefile

Всі dev_tools інтегровані з Makefile для зручності:

```bash
# Переклади
make translations          # Оновити всі
make translations-uk       # Тільки українську
make translations-test     # Перевірити якість
make translations-stats    # Показати статистику
make fix-english          # Виправити англійські переклади
make add-lang LANG=de NAME=Deutsch  # Додати мову

# Docker
make dev                   # Розробка
make prod                  # Продакшн
make build                 # Збірка
```

## Правила розробки dev_tools

### Структура нових інструментів
1. Створіть папку для категорії: `dev_tools/category/`
2. Додайте README.md з документацією
3. Розмістіть скрипти в папці категорії
4. Оновіть головний README.md
5. Додайте команди в Makefile (якщо потрібно)

### Вимоги до скриптів
- Віддавати перевагу Python над bash для кращої підтримки
- Використовувати argparse для обробки аргументів
- Мати кольоровий вивід для зручності
- Перевіряти передумови (файли, середовище)
- Надавати зрозумілі повідомлення про помилки
- Мати документацію в README.md
- Підтримувати команди help

### Приклад структури нового інструменту
```
dev_tools/
└── database/
    ├── README.md
    ├── backup.sh
    ├── restore.sh
    └── migrate.sh
```

## Виключення з Docker

Вся папка `dev_tools/` виключена з Docker збірки через `.dockerignore`:

```dockerignore
# Development tools (не потрапляють в контейнер)
dev_tools/
*.sh
```

Це забезпечує:
- Менший розмір Docker образу
- Безпеку (dev інструменти не потрапляють в продакшн)
- Чистоту контейнера

## Примітки

- Всі скрипти повинні запускатися з кореневої директорії проекту
- Скрипти автоматично активують віртуальне середовище
- Використовуйте відносні шляхи для переносимості
- Додавайте валідацію вхідних параметрів
- Тестуйте скрипти перед комітом