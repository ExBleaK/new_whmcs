# Development Dashboard

Веб-інтерфейс для моніторингу та дослідження Django проекту під час розробки.

## 🎯 Призначення

Development Dashboard - це "Swagger для всього проекту", який надає зручний веб-інтерфейс для:
- Перегляду структури проекту
- Моніторингу стану компонентів
- Дослідження налаштувань та конфігурації
- Тестування URL маршрутів
- Аналізу перекладів та бази даних

## 🔒 Безпека

- **Доступний тільки в DEBUG режимі** (`settings.DEBUG = True`)
- Автоматично відключається в продакшні
- Чутливі дані (паролі, ключі, токени) приховані
- Доступний тільки авторизованим користувачам

## 🚀 Доступ

### URL адреси:
- **Локальна розробка:** `http://localhost:8001/dev/`
- **Docker розробка:** `http://localhost:8000/dev/`
- **З admin панелі:** кнопка "Dev Tools" в навігації (тільки в DEBUG)

### Маршрути:
- `/dev/` - головна сторінка dashboard
- `/dev/urls/` - URL patterns проекту
- `/dev/apps/` - Django додатки та моделі
- `/dev/database/` - інформація про базу даних
- `/dev/translations/` - статус перекладів
- `/dev/settings/` - налаштування Django
- `/dev/system/` - системна інформація

## 📊 Функції Dashboard

### 1. URL Patterns (`/dev/urls/`)
- Показує всі доступні URL маршрути
- Відображає назви маршрутів (name)
- Показує відповідні view функції
- Можливість тестування маршрутів
- Інформація про HTTP методи

### 2. Django Apps (`/dev/apps/`)
- Список всіх встановлених Django додатків
- Категоризація (Custom, Django Core, Dev Tools, Third Party)
- Детальна інформація про моделі
- Кількість полів у кожній моделі
- Шляхи до файлів додатків

### 3. Database Info (`/dev/database/`)
- Інформація про підключення до БД
- Список всіх таблиць
- Статистика (кількість таблиць, користувачів)
- Корисні команди для роботи з БД

### 4. Translations (`/dev/translations/`)
- Статус перекладів для всіх мов
- Прогрес-бари з відсотком готовності
- Інформація про .po та .mo файли
- Статистика перекладених рядків
- Команди для роботи з перекладами

### 5. Settings (`/dev/settings/`)
- Безпечний перегляд налаштувань Django
- Пошук по налаштуваннях
- Приховування чутливих даних
- Інформація про типи значень
- Версії Django та Python

### 6. System Info (`/dev/system/`)
- Інформація про середовище виконання
- Список встановлених додатків
- Middleware stack
- Змінні оточення (з прихованням чутливих)
- Версії компонентів

## 🎨 UI/UX

### Дизайн:
- **Bootstrap 5** - сучасний responsive дизайн
- **Bootstrap Icons** - іконки для кращої навігації
- **Градієнтна бічна панель** - стильний sidebar
- **Кольорове кодування** - різні кольори для різних типів даних

### Навігація:
- **Бічна панель** - швидкий доступ до всіх розділів
- **Активні посилання** - підсвічування поточного розділу
- **Breadcrumbs** - навігаційні хлібні крихти
- **Кнопка повернення** - швидкий перехід назад до додатку

### Інтерактивність:
- **Пошук по налаштуваннях** - JavaScript фільтрація
- **Accordion для додатків** - згортання/розгортання інформації
- **Прогрес-бари** - візуалізація статусу перекладів
- **Кольорові badges** - категоризація та статуси

## 🔧 Технічна реалізація

### Структура файлів:
```
src/dev_dashboard/
├── __init__.py
├── apps.py                 # Конфігурація Django додатку
├── views.py                # Контролери dashboard
├── urls.py                 # URL маршрути
├── models.py               # Моделі (порожні)
├── admin.py                # Admin конфігурація (порожня)
├── tests.py                # Тести (порожні)
└── templates/dev_dashboard/
    ├── base.html           # Базовий шаблон
    ├── dashboard.html      # Головна сторінка
    ├── urls.html           # URL patterns
    ├── apps.html           # Django apps
    ├── database.html       # Database info
    ├── translations.html   # Translations status
    ├── settings.html       # Django settings
    └── system.html         # System information
```

### Інтеграція з проектом:
- Додається до `INSTALLED_APPS` тільки якщо `DEBUG = True`
- URL включається в головний `urls.py` умовно
- Посилання в admin панелі показується тільки в DEBUG режимі

### Безпека даних:
```python
# Приклад приховування чутливих даних
sensitive_keys = ['SECRET_KEY', 'PASSWORD', 'KEY', 'TOKEN', 'API']
for key in dir(settings):
    if any(sensitive in key for sensitive in sensitive_keys):
        safe_settings[key] = '***HIDDEN***'
```

## 🚀 Використання

### Для розробників:
1. **Дослідження проекту** - швидко зрозуміти структуру нового проекту
2. **Налагодження** - перевірити налаштування та стан компонентів
3. **Тестування URL** - швидко протестувати маршрути
4. **Моніторинг перекладів** - відстежувати прогрес локалізації

### Для команди:
1. **Документація** - живий довідник по проекту
2. **Code Review** - швидкий огляд структури та налаштувань
3. **Onboarding** - допомога новим розробникам
4. **Troubleshooting** - діагностика проблем

## 🔮 Майбутні покращення

### Планується додати:
- **Performance Metrics** - моніторинг продуктивності
- **Log Viewer** - перегляд логів в реальному часі
- **Cache Info** - статистика кешування
- **Middleware Analysis** - аналіз middleware stack
- **Template Inspector** - дослідження шаблонів
- **Static Files Info** - інформація про статичні файли
- **Email Testing** - тестування email відправки
- **API Documentation** - автогенерація API документації

### Покращення UI:
- **Dark Theme** - темна тема
- **Export Functions** - експорт даних в JSON/CSV
- **Real-time Updates** - оновлення в реальному часі
- **Mobile Optimization** - кращий мобільний досвід

## 📝 Примітки

- Dashboard працює тільки в DEBUG режимі для безпеки
- Всі чутливі дані автоматично приховуються
- Використовує існуючі Django механізми без додаткових залежностей
- Не впливає на продуктивність продакшн середовища
- Легко розширюється новими функціями